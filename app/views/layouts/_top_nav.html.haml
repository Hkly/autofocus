.top-nav
  .title= link_to "AutoFocus", root_path
  .nav-links
    .pages
      = link_to "About", about_path
    .session-actions
      - if user_signed_in?
        = link_to "Logout", destroy_user_session_path, method: :delete
      - else
        = link_to "Log In", new_user_session_path
        = link_to "Sign Up", new_user_registration_path
